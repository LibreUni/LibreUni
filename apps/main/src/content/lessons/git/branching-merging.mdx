---
title: "Branching and Merging Fundamentals"
course: git
order: 5
module: "Foundations"
description: "The power of Git branching. Understanding branches as pointers, the mechanics of a checkout, and merging strategies (Fast-Forward vs 3-way)."
---

import CodeRunner from '../../../components/CodeRunner';
import CodeExercise from '../../../components/CodeExercise';
import Quiz from '../../../components/Quiz';
import PlantUML from '../../../components/PlantUML.astro';

{/* Source tracking: Lesson narrative synthesized from authoritative documentation and educational references; see References & Further Reading. */}

# The Lightweight Nature of Branches

In many older VCS, branching involved creating a full copy of the source code—which was slow and expensive. In Git, a branch is simply a lightweight, movable pointer to one of the commits in the repository. The default branch name is usually `main`. When you create a new branch, Git creates a new pointer; it does not duplicate any file content.

Each pointer is a tiny file (41 bytes) containing the 40-character SHA-1 checksum of the commit it points to.

<PlantUML theme="none" code={`
@startuml
rectangle "Commit C1" as C1
rectangle "Commit C2" as C2
rectangle "Commit C3" as C3

C1 <- C2
C2 <- C3

card "main" as main
card "feature-x" as feat

main --> C3
feat --> C3
@enduml
`} />

## Creating and Switching Branches

To create a new branch named `testing`:
```bash
git branch testing
```

However, creating a branch does not switch you to it. To start working on that branch, you must "check it out" or "switch" to it:

```bash
git switch testing
```

*(Note: In older tutorials, you will see `git checkout -b testing`. Modern Git recommends `git switch` as it is more intuitive.)*

## The Mechanics of Merging

Merging is the process of bringing changes from one branch into another. There are two primary types of merges you will encounter.

### 1. Fast-Forward Merge
If the branch you are merging into is a direct ancestor of the branch you are merging (i.e., there have been no other commits on the base branch), Git simply moves the pointer forward. No new commit is created.

```bash
git switch main
git merge feature-x
```

### 2. Three-Way Merge
If the history has diverged (i.e., both `main` and `feature-x` have new, different commits), Git performs a three-way merge. It looks at three snapshots:
- The common ancestor (the "Base").
- The tip of Branch A.
- The tip of Branch B.

Git creates a new "Merge Commit" that has two parents.

<PlantUML theme="none" code={`
@startuml
skinparam rectangle {
    BackgroundColor #FFFFFF
}

rectangle "Base (C1)" as C1
rectangle "Main Tip (C2)" as C2
rectangle "Feature Tip (C3)" as C3
rectangle "Merge Commit (C4)" as C4

C1 -> C2
C1 -> C3
C2 -> C4
C3 -> C4
@enduml
`} />

## Introduction to Merge Conflicts

A conflict occurs when the same line of the same file was modified in both branches being merged. Git will stop and ask you to resolve the conflict manually.

```text
<<<<<<< HEAD
printf("Hello from Main\n");
=======
printf("Hello from Feature\n");
>>>>>>> feature-x
```

You must edit the file, choose the correct version (or combine them), remove the markers, and then `git add` and `git commit` to complete the merge.

<Quiz client:load 
  title="Branching Strategy Quiz"
  questions={[
    {
      question: "What actually happens when you create a new branch in Git?",
      options: [
        "Git copies all your files into a new folder.",
        "Git creates a new pointer to the current commit.",
        "Git encrypts the repository to prevent changes.",
        "Git uploads the project to the server."
      ],
      correctIndex: 1,
      explanation: "Git branches are just pointers. Creating one is nearly instantaneous and consumes almost no disk space."
    },
    {
      question: "When does a 'Fast-Forward' merge occur?",
      options: [
        "When the network is very fast.",
        "When there are no new commits on the destination branch since the source branch was created.",
        "When the developer uses the --force flag.",
        "When the merge happens between two different repositories."
      ],
      correctIndex: 1,
      explanation: "A fast-forward merge is possible when the history is linear. Git simply moves the pointer of the destination branch to the tip of the source branch."
    }
  ]}
/>

<CodeExercise client:load
  title="Switching and Creating"
  code={`# Create and immediately switch to 'dev' branch\ngit switch [!blank!] dev`}
  answers={["-c"]}
  explanation="The -c flag (create) for git switch creates the branch and switches your working directory to it in one command."
/>

<CodeRunner client:load
  language="git"
  title="Visualizing Branches"
  code={`# Simulating branch creation and log visualization\ngit init\ngit commit --allow-empty -m "Initial"\ngit branch feature\ngit log --oneline --decorate --all`}
  output="* a1b2c3d (HEAD -> main, feature) Initial"
/>

## References & Further Reading
* [MDX Docs: What is MDX?](https://mdxjs.com/docs/what-is-mdx/) (MIT-licensed docs/site)
* [Astro Docs: Content Collections](https://docs.astro.build/en/guides/content-collections/)
* [OpenStax](https://openstax.org/)
* [MIT OpenCourseWare](https://ocw.mit.edu/)

