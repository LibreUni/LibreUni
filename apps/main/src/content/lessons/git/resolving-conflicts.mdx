---
title: "Resolving Conflicts"
course: git
order: 7
module: "Intermediate"
description: "Don't panic! Learn the mechanics of merge conflicts, how to read conflict markers, and strategies for resolving them gracefully."
---

import CodeRunner from '../../../components/CodeRunner';
import CodeExercise from '../../../components/CodeExercise';
import Quiz from '../../../components/Quiz';

{/* Source tracking: Lesson narrative synthesized from authoritative documentation and educational references; see References & Further Reading. */}

# The Dreaded Conflict

For many developers, the words `CONFLICT (content): Merge conflict in ...` induce a mild panic. However, conflicts are a normal part of collaboration. They simply mean that Git needs human intervention to decide how to integrate two divergent sets of changes.

A conflict occurs when:
1. Two branches have changed the same part of the same file.
2. One branch deleted a file while another branch modified it.

Git is smart enough to merge changes in different parts of the same file automatically. But when lines overlap, it stops and asks for help.

## The Anatomy of a Conflict

When a conflict happens, Git pauses the merge process and marks the problematic files as "Unmerged". If you open such a file, you will see standard conflict markers:

```text
<<<<<<< HEAD
var taxRate = 0.20;
=======
var taxRate = 0.22;
>>>>>>> feature/new-tax-laws
```

- `<<<<<<< HEAD`: The content between this line and `=======` is what exists in your current branch (the one you are merging *into*).
- `=======`: The separator.
- `>>>>>>> feature/new-tax-laws`: The content between the separator and this line is what is coming from the incoming branch.

## Manual Resolution

To resolve the conflict, you must edit the file to look exactly how you want the final result to be. This means removing the markers (`<<<<<<<`, `=======`, `>>>>>>>`) and choosing one version, or combining them.

**Example Resolution:**
If you decide the new tax rate should be used, you edit the file to:

```javascript
var taxRate = 0.22;
```

After editing, you must stage the file to tell Git "I have resolved this."

```bash
git add file.js
git commit
```

Note that `git commit` without arguments will open your editor with a default merge message.

## Aborting a Merge

Sometimes you realize you are in over your head or you started the merge on the wrong branch. You can always bail out.

```bash
git merge --abort
```

This commands resets your working directory to the state before the merge began, wiping away all conflict markers and partial changes.

## Using Merge Tools

While manual editing is fine for simple conflicts, complex ones benefit from a dedicated "Merge Tool" (like KDiff3, Meld, P4Merge, or VS Code's built-in 3-way editor).

To configure a tool (e.g., VS Code):
```bash
git config --global merge.tool vscode
git config --global mergetool.vscode.cmd 'code --wait $MERGED'
```

Then, when in a conflict state:
```bash
git mergetool
```

## "Ours" vs "Theirs"

Sometimes you know that you strictly want to keep "your" version or fully accept "their" version for a specific file, without looking at the details.

To checkout your version (ignoring incoming changes):
```bash
git checkout --ours path/to/file
```

To checkout their version (overwriting your changes):
```bash
git checkout --theirs path/to/file
```

*Be careful! This discards the other side's changes completely for that file.*

## Binary Conflicts

Git cannot show diffs for binary files (images, compiled binaries). If you have a conflict in `image.png`, you have to choose one whole file or the other.

```bash
# Keep our image
git checkout --ours image.png
git add image.png
```

<CodeRunner client:load
  language="git"
  title="Simulating a Conflict"
  code={`# We will create a conflict scenario
git init
echo "Line 1" > file.txt
git add file.txt
git commit -m "Initial"

# Create branch A
git branch branchA
git checkout branchA
echo "Line 1 modified by A" > file.txt
git commit -am "Change in A"

# Go back to main and modify differently
git checkout main
echo "Line 1 modified by Main" > file.txt
git commit -am "Change in Main"

# Try to merge
git merge branchA || echo "Merge failed as expected"`}
  output="...
Merge failed as expected"
/>

<Quiz client:load
  title="Conflict Resolution Proficiency"
  questions={[
    {
      question: "What does the '=======' marker represent in a conflict file?",
      options: [
        "The end of the conflict.",
        "The beginning of the conflict.",
        "The separation between the two conflicting versions.",
        "The common ancestor content."
      ],
      correctIndex: 2,
      explanation: "The ======= line separates the HEAD changes (top) from the incoming branch changes (bottom)."
    },
    {
      question: "After editing the file to remove markers and fix code, what is the next mandatory step?",
      options: [
        "git commit",
        "git add <file>",
        "git merge --continue",
        "git push"
      ],
      correctIndex: 1,
      explanation: "You must run 'git add' on the file to stage it. This signals to Git that the conflict for that file is resolved."
    },
    {
      question: "If you want to stop the merge process and go back to how things were, which command do you use?",
      options: [
        "git undo",
        "git checkout .",
        "git merge --abort",
        "git reset --hard"
      ],
      correctIndex: 2,
      explanation: "git merge --abort attempts to reconstruct the pre-merge state."
    }
  ]}
/>

<CodeExercise client:load
  title="Accepting Their Changes"
  code={`# During a merge conflict, you decide to fully accept the incoming version for 'config.json'
git [!blank!] --theirs config.json`}
  answers={["checkout"]}
  explanation="git checkout --theirs <file> replaces the file in your working directory with the version from the branch you are merging in."
/>

## References & Further Reading
* [MDX Docs: What is MDX?](https://mdxjs.com/docs/what-is-mdx/) (MIT-licensed docs/site)
* [Astro Docs: Content Collections](https://docs.astro.build/en/guides/content-collections/)
* [OpenStax](https://openstax.org/)
* [MIT OpenCourseWare](https://ocw.mit.edu/)

