---
title: "Stashing and Cleaning"
course: git
order: 10
module: "Intermediate"
description: "Need to switch context quickly? Learn how to shelve your work with git stash and remove untracked garbage with git clean."
---

import CodeRunner from '../../../components/CodeRunner';
import CodeExercise from '../../../components/CodeExercise';
import Quiz from '../../../components/Quiz';

# The Interrupt Driven Workflow

You are deep in the zone, coding a complex feature. Suddenly, a critical bug is reported in production. You need to switch to the `main` branch to fix it, but your current working directory is a mess of half-finished changes.

You can't commit, because the code doesn't compile. You can't just switch branches, because Git will forbid it if your changes conflict with the destination branch.

Enter `git stash`.

## Stashing Changes

Stashing takes the dirty state of your working directory (modified tracked files and staged changes) and saves it on a stack of unfinished changes that you can reapply at any time.

```bash
git stash
```

Your working directory is now clean (matching the HEAD commit). You can safely switch branches, fix the bug, and commit.

### Naming Stashes
By default, stashes are named "WIP on branch...". To make them easier to find later:

```bash
git stash push -m "Experimenting with new login logic"
```

## Retrieving Stashes

Once you are done with the bug fix and back on your feature branch, you want your changes back.

### Pop
`git stash pop` removes the latest stash from the stack and applies it to your working directory.

```bash
git stash pop
```

### Apply
If you want to apply the stash but keep it in the stack (e.g., to apply it to multiple branches):

```bash
git stash apply
```

## Managing the Stack

You can have multiple stashes.

```bash
git stash list
```

Output:
```text
stash@{0}: On feature: Experimenting with new login logic
stash@{1}: WIP on main: 4d3e2f1 Fix typo
```

To apply a specific stash:
```bash
git stash apply stash@{1}
```

To delete a stash:
```bash
git stash drop stash@{0}
```

To clear all stashes:
```bash
git stash clear
```

## Stashing Untracked Files

By default, `git stash` only saves tracked files. If you have created new files (untracked), they will remain in your working directory. To stash them as well:

```bash
git stash -u
```
(or `--include-untracked`)

## Cleaning the Workplace

Sometimes you just want to destroy untracked files. Maybe your build script generated a thousand `.o` files and you want to start fresh.

`git clean` removes untracked files from the working directory.

**Dry Run:** Always do this first to see what *would* be deleted.
```bash
git clean -n
```

**Force Clean:** Actually delete the files.
```bash
git clean -f
```

**Directories too:**
```bash
git clean -fd
```

**Ignored files:** To remove ignored files as well (e.g., `node_modules`):
```bash
git clean -fdX
```

<CodeRunner client:load
  language="git"
  title="Stash Workflow"
  code={`# Simulate a dirty state
git init
echo "base" > file.txt
git add file.txt
git commit -m "Base"
echo "work in progress" >> file.txt

# Stash it
git stash
cat file.txt # Should be just "base"

# Bring it back
git stash pop
cat file.txt # Should have "work in progress"`}
  output="Saved working directory and index state...\nbase\nDropped refs/stash@{0}...\nbase\nwork in progress"
/>

<Quiz client:load
  title="Stash Proficiency"
  questions={[
    {
      question: "What is the difference between 'git stash pop' and 'git stash apply'?",
      options: [
        "pop deletes the stash after applying it; apply keeps it.",
        "apply deletes the stash after applying it; pop keeps it.",
        "pop works on untracked files; apply works on tracked files.",
        "There is no difference."
      ],
      correctIndex: 0,
      explanation: "Pop is like removing the top card from a deck. Apply looks at the card but leaves it there."
    },
    {
      question: "How do you stash untracked files?",
      options: [
        "git stash --all",
        "git stash -u",
        "git stash --force",
        "git stash save"
      ],
      correctIndex: 1,
      explanation: "The -u or --include-untracked flag tells stash to include files that haven't been added to Git yet."
    }
  ]}
/>

<CodeExercise client:load
  title="Listing Stashes"
  code={`# You want to see the list of all saved stashes to find an old experiment.\ngit stash [!blank!]`}
  answers={["list"]}
  explanation="git stash list shows the stack of stashed changes."
/>
