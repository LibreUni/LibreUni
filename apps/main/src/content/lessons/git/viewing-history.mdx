---
title: "Inspecting History"
course: git
description: "Go beyond basic logging. Learn to filter history, visualize branches, use git blame, and inspect specific commits with git show."
---
import CodeRunner from '../../../components/CodeRunner';
import CodeExercise from '../../../components/CodeExercise';
import Quiz from '../../../components/Quiz';

# The Power of the Log

As a project grows, the history becomes a massive database of changes. `git log` is the query language for this database. Using it effectively can save you hours of debugging.

## Formatting the Log

The default `git log` is verbose. To get a high-level overview, you can customize the output format.

### One-line
```bash
git log --oneline
```
This shows just the abbreviated SHA and the commit message.

### Decorating
To see where branches and tags are pointing:
```bash
git log --oneline --decorate
```

### Graphing
To visualize the branching history in the terminal:
```bash
git log --oneline --graph --all
```
This draws an ASCII art tree of your commit history, showing merges and divergences.

## Filtering History

You often want to find specific commits rather than listing everything.

### By Time
```bash
git log --since="2 weeks ago"
git log --until="2023-01-01"
```

### By Author
```bash
git log --author="Linus"
```

### By File
To see only commits that modified a specific file:
```bash
git log -- path/to/file.py
```

### By Content (Search)
To search for commits that added or removed a specific string (the "Pickaxe"):
```bash
git log -S "functionName"
```

## Inspecting Commits

If `git log` lists the commits, `git show` inspects the details of a single object.

```bash
git show a1b2c3d
```
This displays the log message and the diff of what changed in that commit.

## Blaming

When you encounter a bug, you often want to know: "Who wrote this line and when?" `git blame` annotates each line of a file with the commit information.

```bash
git blame main.c
```

Output:
```text
^4a2b3c (Alice 2023-01-01 10:00:00 +0000 1) int main() {
8d7e6f (Bob   2023-01-02 11:00:00 +0000 2)     return 0;
^4a2b3c (Alice 2023-01-01 10:00:00 +0000 3) }
```

In many IDEs, this functionality is integrated as "Git Lens" or "Annotate".

## Reflog: The Safety Net

Git keeps a log of where your HEAD pointer has been, even if you deleted branches or reset commits. This is called the "Reference Log" or `reflog`.

```bash
git reflog
```

It shows a local history of your actions (switching branches, resetting, committing). It is invaluable for recovering "lost" commits.

<CodeRunner client:load
  language="git"
  title="Git Log Filtering"
  code={`# Initialize and create some history
git init
echo "one" > file.txt
git add file.txt
git commit -m "First commit"
echo "two" >> file.txt
git commit -am "Second commit"
echo "three" >> file.txt
git commit -am "Third commit"

# Show only the last 2 commits
git log -n 2 --oneline`}
  output="... Third commit\n... Second commit"
/>

## Diffing

We've used `git diff` to see working directory changes. But it is much more powerful.

### Comparing Branches
To see what is in `feature` that is not in `main`:
```bash
git diff main..feature
```

### Comparing Commits
To see the difference between two specific points in time:
```bash
git diff HEAD~2 HEAD
```

<Quiz client:load
  title="History Inspection Quiz"
  questions={[
    {
      question: "Which command would you use to find out which commit introduced the string 'TODO: Fix this'?",
      options: [
        "git log --grep='TODO: Fix this'",
        "git log -S 'TODO: Fix this'",
        "git search 'TODO: Fix this'",
        "git blame 'TODO: Fix this'"
      ],
      correctIndex: 1,
      explanation: "git log -S (the pickaxe) searches for differences that introduce or remove an instance of the specified string."
    },
    {
      question: "What does 'git blame' show?",
      options: [
        "It shows who broke the build.",
        "It shows the commit, author, and time for the last modification of each line in a file.",
        "It shows a list of all developers on the project.",
        "It sends an email to the author of a bug."
      ],
      correctIndex: 1,
      explanation: "git blame annotates the file line-by-line with revision information."
    }
  ]}
/>

<CodeExercise client:load
  title="Visualizing the Graph"
  code={`# Complete the command to show a decorated, one-line graph of all branches\ngit log --oneline --decorate --graph --[!blank!]`}
  answers={["all"]}
  explanation="The --all flag ensures that we see the history of all branches, not just the current one."
/>
