---
title: "Testing and Quality Signals"
course: libreuni
order: 4
module: "Quality"
description: "How contributors use builds, previews, accessibility checks, and visual inspection to keep LibreUni reliable."
---

import PlantUML from '../../../components/PlantUML.astro';
import TikZ from '../../../components/TikZ.astro';
import CodeRunner from '../../../components/CodeRunner';
import Quiz from '../../../components/Quiz';
import CaseStudy from '../../../components/CaseStudy';

# Testing and Quality Signals

LibreUni quality is not one test. It is a chain of signals: content reads cleanly, MDX compiles, diagrams render, interactive widgets hydrate, the layout works on mobile and desktop, and the lesson has a clear learning purpose.

## Quality Pipeline

<PlantUML theme="none" code={`
skinparam backgroundColor transparent
rectangle "Author edits" as Edit
rectangle "MDX compile" as Compile
rectangle "Diagram render" as Diagrams
rectangle "Static build" as Build
rectangle "Browser preview" as Preview
rectangle "Review" as Review

Edit --> Compile
Compile --> Diagrams
Diagrams --> Build
Build --> Preview
Preview --> Review
`} />

## Visual Sanity Checks

For diagrams, the key quality question is simple: can a reader understand the structure without fighting the layout? The overlay exists so large diagrams can be inspected without squeezing text into unreadable shapes.

<TikZ code={`
\\foreach \\x/\\label in {0/Content,2/Build,4/Preview,6/Review} {
  \\draw[rounded corners=4pt, fill=blue!8, draw=blue!60, thick] (\\x,0) rectangle +(1.35,0.7);
  \\node at (\\x + 0.675,0.35) {\\small \\label};
}
\\draw[->, thick] (1.35,0.35) -- (2,0.35);
\\draw[->, thick] (3.35,0.35) -- (4,0.35);
\\draw[->, thick] (5.35,0.35) -- (6,0.35);
\\draw[rounded corners=6pt, dashed, draw=green!60!black, thick] (-0.25,-0.25) rectangle (7.6,0.95);
\\node[below] at (3.65,-0.25) {\\small Every step should produce a useful signal};
`} />

## Browser Logic Example

The code runner can execute JavaScript snippets too. Use this for small algorithmic demonstrations where Python would be unnecessary.

<CodeRunner client:load
  language="javascript"
  title="JavaScript Check"
  code={`const checks = ["build", "preview", "review"];
const status = Object.fromEntries(checks.map((name) => [name, "ready"]));

console.log(JSON.stringify(status, null, 2));
console.log("All checks named:", Object.keys(status).join(" -> "));`}
/>

## Python Data Sketch

This example visualizes a fictional quality dashboard. It is intentionally small, but it exercises the same runtime used by scientific and math lessons.

<CodeRunner client:load
  title="Quality Signal Chart"
  code={`import matplotlib.pyplot as plt

labels = ["MDX", "Diagrams", "Hydration", "Mobile", "Review"]
scores = [98, 95, 92, 90, 96]

plt.figure(figsize=(6, 3.2))
bars = plt.bar(labels, scores, color=["#2563eb", "#7c3aed", "#059669", "#f59e0b", "#0f172a"])
plt.ylim(80, 100)
plt.ylabel("confidence")
plt.title("Example Quality Signals")
for bar, score in zip(bars, scores):
    plt.text(bar.get_x() + bar.get_width() / 2, score + 0.5, str(score), ha="center")
print("Quality signal chart rendered.")
`} />

## Review Heuristics

Use this checklist when reviewing a new lesson:

1. The title and description match the actual content.
2. The lesson teaches one coherent idea.
3. Diagrams are legible in the page and in the overlay.
4. Interactive components have a reason to be there.
5. Code samples are short enough to inspect.
6. The page builds without generating new errors.

<CaseStudy client:load
  title="Quality Triage"
  scenario="A lesson builds successfully, but one diagram is too dense, the quiz checks trivia instead of the learning goal, and a code sample prints twenty lines of unformatted output."
  question="Which review response is most useful?"
  options={[
    "Approve because the build passes",
    "Request targeted changes: simplify the diagram, align the quiz with the goal, and trim the output",
    "Reject the entire course",
    "Ignore the issue because users can zoom their browser"
  ]}
  correctIndex={1}
  explanation="Mechanical success is only one signal. Review should connect usability, teaching value, and maintainability."
/>

<Quiz client:load
  title="Quality Check"
  question="What is the best reason to keep this course in the project?"
  options={[
    "It replaces documentation entirely",
    "It demonstrates platform capabilities while giving contributors a useful orientation path",
    "It proves all future lessons will be correct",
    "It avoids the need for local testing"
  ]}
  correctIndex={1}
  explanation="The course is both a contributor guide and a living test surface for the platform."
/>

