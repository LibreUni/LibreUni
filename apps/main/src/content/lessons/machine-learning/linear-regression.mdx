---
title: "Linear Regression"
course: machine-learning
description: "Ordinary Least Squares parameter estimation using the closed-form analytical Normal Equation."
---
import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';

# Linear Regression and the Normal Equation

Linear Regression models a continuous target $y$ as a linear combination of input features $x$:

$$ \hat{y} = \theta^T \mathbf{x} $$

where $\theta$ is the model parameter vector (including bias $\theta_0$), and $\mathbf{x}$ is the feature vector (with $x_0 = 1$).

## Parameter Estimation: Ordinary Least Squares

To train the model, we find parameters $\theta$ that minimize the Mean Squared Error (MSE) over the dataset:

$$ \text{MSE}(X, h_{\theta}) = \frac{1}{m} \sum_{i=1}^{m} (\theta^T \mathbf{x}^{(i)} - y^{(i)})^2 $$

### The Closed-Form Normal Equation

To minimize the cost function, we solve analytically using the **Normal Equation**:

$$ \hat{\theta} = (X^T X)^{-1} X^T y $$

- $X$: The design matrix of shape $(m, n+1)$ containing all features.
- $y$: The target vector of shape $(m, 1)$.

### Multicollinearity and SVD

If the matrix $X^T X$ is singular (non-invertible) due to redundant, highly correlated features, standard inversion fails. Solvers compute the pseudo-inverse $X^+$ using Singular Value Decomposition (SVD) for stability.

### Computational Complexity

Computing $(X^T X)^{-1}$ requires inverting an $(n+1) \times (n+1)$ matrix. The computational complexity is between $O(n^{2.4})$ and $O(n^3)$, making the Normal Equation expensive when the number of features is large.

## Example: Computing the Normal Equation

The following example demonstrates computing model coefficients using the Normal Equation:

<CodeRunner client:load
  description="Compute model parameters analytically using the Normal Equation. Alter the synthetic target equation coefficients to see if the equation adapts and finds the new parameters."
  code={`import numpy as np

# Generate synthetic linear data with random Gaussian noise
np.random.seed(42)
X = 2 * np.random.rand(5, 1)
y = 4 + 3 * X + np.random.randn(5, 1)

# Add bias column (x0 = 1) to build design matrix X_b
X_b = np.c_[np.ones((5, 1)), X]

# Solve analytically via normal equation: theta = (X^T * X)^(-1) * X^T * y
theta_best = np.linalg.inv(X_b.T.dot(X_b)).dot(X_b.T).dot(y)
print("Estimated coefficients [bias, weight]:")
print(theta_best.flatten())`}
  output="Estimated coefficients [bias, weight]:
[3.9877443  3.40798606]"
/>

## Exercise

Test your understanding of the analytical parameter estimation limits:

<Quiz client:load
  question="If your dataset contains 100,000 features and 1,000 samples, what makes the Normal Equation less suitable than gradient descent?"
  options={[
    "The target vector cannot have more rows than the design matrix.",
    "The matrix inversion term (X^T X)^-1 has dimensions of 100,001 by 100,001, making its direct inversion extremely slow or computationally intractable.",
    "The Normal Equation works only when there are more samples than features.",
    "Normal Equation parameters are prone to local minima."
  ]}
  correctIndex={1}
  explanation="The Normal Equation requires inverting the matrix (X^T X), which has dimensions of (n+1) by (n+1). With n = 100,000, this requires inverting a 100,000x100,000 matrix, leading to O(n^3) complexity, which is computationally infeasible."
/>

{/* Source tracking: The Normal Equation, MSE minimization, and computational complexity synthesized from 'Hands-On Machine Learning (3rd Edition)' by Aurélien Géron, Chapter 4, and 'An Introduction to Machine Learning' by Smola & Vishwanathan, Chapter 8. */}

## References & Further Reading
*   [Géron, A. (2022). *Hands-On Machine Learning with Scikit-Learn, Keras, and TensorFlow* (3rd ed.). O'Reilly Media.](https://www.oreilly.com/library/view/hands-on-machine-learning/9781098125967/)
*   [Smola, A., & Vishwanathan, S. V. N. (2008). *An Introduction to Machine Learning*. Cambridge University Press.](https://vlad.smola.org/books/Introduction-to-Machine-Learning.pdf)
