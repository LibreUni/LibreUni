---
title: "Logistic Regression"
course: machine-learning
description: "Binary classification modeling using Sigmoid probabilistic mapping and Cross-Entropy optimization."
---
import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';
import PythonDiagram from '../../../components/PythonDiagram.astro';

# Logistic Regression and Probability Estimation

Logistic Regression is a classifier that computes a weighted sum of inputs and maps the output to a probability between $0$ and $1$.

## Probabilistic Formulation: The Sigmoid Function

The model feeds its linear output into the **Sigmoid Function** $\sigma(t)$:

$$ \hat{p} = \sigma(\theta^T \mathbf{x}) = \frac{1}{1 + e^{-\theta^T \mathbf{x}}} $$

The prediction rule for binary classes is:

$$ \hat{y} = \begin{cases} 1 & \text{if } \hat{p} \ge 0.5 \\ 0 & \text{if } \hat{p} < 0.5 \end{cases} $$

The prediction decision boundary is linear where $\theta^T \mathbf{x} = 0$.

<PythonDiagram
  title="The Sigmoid Function"
  code={`import os
import numpy as np
import matplotlib.pyplot as plt
t = np.linspace(-6, 6, 200)
sig = 1 / (1 + np.exp(-t))
plt.figure(figsize=(5, 3.5))
plt.plot(t, sig, "b-", linewidth=2, label=r"$\\sigma(t) = \\frac{1}{1 + e^{-t}}$")
plt.axhline(y=0.5, color="gray", linestyle="--")
plt.axvline(x=0, color="gray", linestyle="--")
plt.title("Sigmoid Curve")
plt.grid(True, alpha=0.3)
plt.legend()
plt.tight_layout()
plt.savefig(os.environ["LIBREUNI_OUTPUT"], format="svg")`}
/>

## Parameter Optimization: Log Loss

To optimize parameters $\theta$, we minimize the convex **Log Loss** (binary cross-entropy):

$$ J(\theta) = -\frac{1}{m} \sum_{i=1}^{m} \left[ y^{(i)} \log(\hat{p}^{(i)}) + (1 - y^{(i)}) \log(1 - \hat{p}^{(i)}) \right] $$

It penalizes confident incorrect predictions with infinite cost (if $y=1$ and $\hat{p} \to 0$, then $-\log(\hat{p}) \to \infty$).

### Softmax Regression

For multi-class classification, **Softmax Regression** generalizes this by normalizing raw logits into a probability distribution across classes.

## Example: Logistic Regression Inference

The following example demonstrates calculating predictions using a trained Logistic Regression classifier:

<CodeRunner client:load
  description="Perform logistic regression inference. Calculate the predicted class and class probabilities for a sample point near the decision boundary."
  code={`import numpy as np
from sklearn.linear_model import LogisticRegression

# Create toy dataset
X = np.array([[1], [2], [3], [4], [5]])
y = np.array([0, 0, 0, 1, 1])

# Train model
clf = LogisticRegression()
clf.fit(X, y)

# Predict probabilities and classes
test_val = np.array([[3.5]])
prob = clf.predict_proba(test_val)
pred = clf.predict(test_val)

print(f"Probabilities [0, 1]: {prob[0].round(4)}")
print(f"Class: {pred[0]}")`}
  output="Probabilities [0, 1]: [0.3235 0.6765]
Class: 1"
/>

## Exercise

Test your knowledge of the Logistic Regression model boundary behavior:

<Quiz client:load
  question="Why does the Log Loss cost function lack a closed-form analytical solution like the Normal Equation?"
  options={[
    "Because the linear transformation outputs are non-convex.",
    "Because the sigmoid function introduces a non-linear exponential term in the cost function, preventing linear algebraic isolation of the parameters.",
    "Because classification labels are discrete integers.",
    "Because gradient descent always finds local minima."
  ]}
  correctIndex={1}
  explanation="The sigmoid function's exponential term prevents solving for the parameters analytically by setting the derivative of the cost function to zero. Instead, numerical optimization techniques (like Gradient Descent) must be used."
/>

{/* Source tracking: Sigmoid mapping, Log-loss cost, and parameter derivative equations synthesized from 'Hands-On Machine Learning (3rd Edition)' by Aurélien Géron, Chapter 4. */}

## References & Further Reading
*   [Géron, A. (2022). *Hands-On Machine Learning with Scikit-Learn, Keras, and TensorFlow* (3rd ed.). O'Reilly Media.](https://www.oreilly.com/library/view/hands-on-machine-learning/9781098125967/)
