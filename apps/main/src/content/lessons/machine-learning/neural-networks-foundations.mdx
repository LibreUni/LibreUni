---
title: "Neural Networks Foundations"
course: machine-learning
description: "Foundational architecture of artificial neural networks, backpropagation, and activation functions."
---
import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';

# Neural Networks Foundations

Artificial Neural Networks (ANNs) are computational models inspired by biological neural networks, structured as layers of interconnected nodes.

## The Perceptron

The Perceptron is the simplest ANN architecture, computing a weighted sum of inputs plus a bias, and applying a step function:

$$ \hat{y} = \text{step}(z) = \text{step}(w^T x + b) $$

A single Perceptron is a linear classifier, meaning it cannot solve non-linearly separable problems (like the XOR problem).

## Multilayer Perceptron (MLP)

An MLP consists of an input layer, one or more hidden layers, and an output layer. Hidden layers allow the model to learn non-linear representations.

### Forward Propagation
For layer $l$, the activations $a^{(l)}$ are computed using the weight matrix $W^{(l)}$ and bias vector $b^{(l)}$:

$$ z^{(l)} = W^{(l)} a^{(l-1)} + b^{(l)} $$
$$ a^{(l)} = \phi(z^{(l)}) $$

where $\phi$ is the activation function.

### Activation Functions
Non-linear activation functions prevent stacked layers from collapsing into a single linear model:
- **Sigmoid**: $\sigma(z) = 1 / (1 + e^{-z})$, squashing outputs to $(0, 1)$.
- **Tanh**: $\tanh(z) = (e^z - e^{-z}) / (e^z + e^{-z})$, squashing to $(-1, 1)$.
- **ReLU**: $\text{ReLU}(z) = \max(0, z)$, preventing vanishing gradients.
- **ELU**: $\text{ELU}(z) = \max(\alpha(e^z - 1), z)$, preventing dying neurons with small negative values.

## Backpropagation

Backpropagation trains networks by computing the gradient of the loss function with respect to the weights using the **chain rule** of calculus, working backward from the output layer:

$$ \frac{\partial L}{\partial W^{(l)}} = \frac{\partial L}{\partial z^{(l)}} (a^{(l-1)})^T $$

The error term $\delta^{(l)} = \frac{\partial L}{\partial z^{(l)}}$ is computed recursively from the output error using matrix multiplications.

## Example: Forward Pass Calculations

The following example demonstrates calculating the forward pass of a single neuron using NumPy:

<CodeRunner client:load
  description="Compute the forward pass of a single neuron using weights, biases, and a ReLU activation function. Modify the input values to see if the neuron activates."
  code={`import numpy as np

# Inputs, weights, and bias
x = np.array([1.5, 2.0])
w = np.array([0.8, -0.5])
b = -0.2

# Linear output
z = np.dot(w, x) + b

# ReLU activation
activation = max(0, z)
print(f"Pre-activation z: {z:.2f}")
print(f"Activation output: {activation:.2f}")`}
  output="Pre-activation z: 0.00
Activation output: 0.00"
/>

## Exercise

Test your understanding of activation functions:

<Quiz client:load
  question="Why did the deep learning community transition from Sigmoid activations to ReLU activations in hidden layers?"
  options={[
    "Because ReLU functions are bounded between -1 and 1.",
    "Because Sigmoid functions suffer from the vanishing gradient problem, where gradients become close to zero for large inputs, slowing down training.",
    "Because ReLU is a non-linear function while Sigmoid is linear.",
    "Because Sigmoid activations make backpropagation mathematically impossible."
  ]}
  correctIndex={1}
  explanation="For very positive or negative inputs, the slope of the Sigmoid function is close to zero. During backpropagation, multiplying by these small gradients causes them to vanish in early layers. ReLU has a constant gradient of 1 for all positive inputs, mitigating this problem."
/>

{/* Source tracking: Perceptron logic, MLP forward propagation, activation functions, and backpropagation chain rule synthesized from 'Hands-On Machine Learning (3rd Edition)' by Aurélien Géron, Chapter 10. */}

## References & Further Reading
*   [Géron, A. (2022). *Hands-On Machine Learning* (3rd ed.). O'Reilly Media.](https://www.oreilly.com/library/view/hands-on-machine-learning/9781098125967/)
