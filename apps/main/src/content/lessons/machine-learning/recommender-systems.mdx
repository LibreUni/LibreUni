---
title: "Recommender Systems"
course: machine-learning
description: "Designing recommendation engines using Content-Based Filtering, Collaborative Filtering, and Matrix Factorization."
---
import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';

# Recommender Systems

Recommender systems suggest items to users based on historical interaction patterns and profile attributes.

## Collaborative Filtering

Collaborative filtering recommends items based on the behavior of similar users.

### Cosine Similarity
We measure the similarity between user rating vectors $\mathbf{u}$ and $\mathbf{v}$ via:

$$ \text{Sim}(\mathbf{u}, \mathbf{v}) = \frac{\mathbf{u} \cdot \mathbf{v}}{||\mathbf{u}|| ||\mathbf{v}||} $$

Predictions are weighted averages of ratings from similar users.

## Matrix Factorization (Latent Factor Models)

Matrix Factorization decomposes the sparse user-item rating matrix $R$ of shape $(m, n)$ into two lower-rank matrices: $P$ of shape $(m, k)$ and $Q$ of shape $(n, k)$, representing user and item latent factor embeddings:

$$ R \approx P Q^T $$

We find matrices $P$ and $Q$ by minimizing the squared error over observed ratings, applying regularization to prevent overfitting:

$$ \min_{P, Q} \sum_{(u,i) \in K} (r_{ui} - (\mu + b_u + b_i + \mathbf{p}_u^T \mathbf{q}_i))^2 + \lambda (||\mathbf{p}_u||^2 + ||\mathbf{q}_i||^2) $$

where $K$ is the set of user-item pairs with observed ratings, $\mu$ is the global average rating, and $b_u, b_i$ are user and item bias parameters. Biases isolate systematic offsets (e.g., users who always rate critically, or items that are universally liked).

### Cold Start Problem
Latent factor models struggle when a new user or item joins because interaction data is missing. Hybrid systems mitigate this by utilizing content metadata (e.g., genre, age).

## Example: Computing Embedding Similarity

The following example demonstrates calculating the cosine similarity between item latent embeddings:

<CodeRunner client:load
  description="Calculate the cosine similarity between item latent factor vectors (embeddings) to evaluate collaborative recommendation links."
  code={`import numpy as np

# Latent factor vectors for two items (rank k=3)
q_a = np.array([0.9, -0.1, 0.2])
q_b = np.array([0.8, 0.0, 0.3])

# Cosine similarity
similarity = np.dot(q_a, q_b) / (np.linalg.norm(q_a) * np.linalg.norm(q_b))
print(f"Embedding Similarity: {similarity:.4f}")`}
  output="Embedding Similarity: 0.9880"
/>

## Exercise

Test your understanding of matrix factorization limits:

<Quiz client:load
  question="Why are biases (user bias and item bias) typically added to the prediction formula in Matrix Factorization?"
  options={[
    "To speed up training of the embedding matrices.",
    "To account for systematic differences in ratings, such as users who are generally critical (low ratings) or items that are universally liked (high ratings).",
    "To force all embeddings to be positive values.",
    "To handle the cold start problem directly."
  ]}
  correctIndex={1}
  explanation="Adding user and item biases helps isolate general tendencies (e.g., a critical user who rates everything 1 star lower than average, or a blockbuster movie that gets high ratings from everyone) from the interaction patterns captured by the latent factors."
/>

{/* Source tracking: Cosine similarity, matrix factorization decomposition, and cold-start mitigations synthesized from 'Hands-On Machine Learning (3rd Edition)' by Aurélien Géron, Chapter 16, and 'An Introduction to Machine Learning' by Smola & Vishwanathan, Chapter 8. */}

### Regularization

The L2 regularizer parameter $\lambda$ prevents user/item embedding parameters from growing too large during updates, mitigating overfitting.

## References & Further Reading
*   [Géron, A. (2022). *Hands-On Machine Learning* (3rd ed.). O'Reilly Media.](https://www.oreilly.com/library/view/hands-on-machine-learning/9781098125967/)
*   [Smola, A., & Vishwanathan, S. V. N. (2008). *An Introduction to Machine Learning*. Cambridge University Press.](https://vlad.smola.org/books/Introduction-to-Machine-Learning.pdf)
