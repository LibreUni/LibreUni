---
title: Algebraic Numbers
course: math-foundations
order: 11
module: "Advanced Number Theory"
description: Algebraic vs transcendental numbers, minimal polynomials, and algebraic integers.
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';
import TikZ from '../../../components/TikZ.astro';
import PythonDiagram from '../../../components/PythonDiagram.astro';

# Algebraic Numbers

<TikZ
  cdn={true}
  code={String.raw`\begin{tikzpicture}[scale=1]
  \draw[thick, rounded corners] (0,0) rectangle (6,3);
  \node at (3,2.5) {This lesson's core structure};
  \node[draw, fill=blue!10] at (1.6,1.4) {Definitions};
  \node[draw, fill=green!10] at (3.0,1.4) {Methods};
  \node[draw, fill=orange!10] at (4.4,1.4) {Applications};
\end{tikzpicture}`}
/>

{/* Source tracking: Core definitions and theorem statements in this lesson were synthesized from standard open educational references and should be refined with topic-specific primary sources during future maintenance. */}


Algebraic numbers are complex numbers that are roots of non-zero polynomials with rational coefficients.

## Definition and Minimal Polynomials

A number $\alpha \in \mathbb{C}$ is **algebraic** if there exists a polynomial $P(x) \in \mathbb{Q}[x]$ such that $P(\alpha) = 0$. The unique monic polynomial of lowest degree that $\alpha$ satisfies is called its **minimal polynomial**.

<CodeRunner client:load 
  code={`def evaluate_rational_poly(coeffs, x):
    res = 0
    for i, c in enumerate(coeffs):
        res += c * (x**i)
    return res

# Check if i (sqrt(-1)) is algebraic
# Minimal poly: x^2 + 1
coeffs_i = [1, 0, 1]
import math
val = evaluate_rational_poly(coeffs_i, 1j)
print(f"p(i) = {val}")

# Check if sqrt(2) + sqrt(3) is algebraic
# It satisfies x^4 - 10x^2 + 1 = 0
alpha = math.sqrt(2) + math.sqrt(3)
coeffs_alpha = [1, 0, -10, 0, 1]
print(f"p(sqrt(2)+sqrt(3)) = {evaluate_rational_poly(coeffs_alpha, alpha)}")`}
/>

## Algebraic Integers

An **algebraic integer** is a root of a monic polynomial with integer coefficients. For example, $\sqrt{2}$ is an algebraic integer, but $1/2$ is not (it is an algebraic number, but its minimal polynomial is $2x - 1$, which is not monic).

<Quiz client:load 
  question="Is every rational number an algebraic number?"
  options={[
      { id: '1', text: "Yes, because q = a/b is a root of bx - a = 0.", isCorrect: true },
      { id: '2', text: "No, only integers are algebraic.", isCorrect: false },
      { id: '3', text: "Only if the denominator is prime.", isCorrect: false }
  ]}
/>

## Transcendental Numbers

Numbers that are not algebraic are called **transcendental**. Proving a number is transcendental is usually much harder. Famous examples include $\pi$ and $e$.

<CodeRunner client:load 
  code={`import math

# Liouville's constant is transcendental
liouville = sum(10**(-math.factorial(n)) for n in range(1, 10))
print(f"Liouville's constant approx: {liouville}")
print("Transcendental numbers are 'uncountably' more common than algebraic ones.")`}
/>

<Quiz client:load 
  question="What is the minimal polynomial of the golden ratio phi = (1 + sqrt(5))/2?"
  options={[
      { id: '1', text: "x^2 - x - 1", isCorrect: true },
      { id: '2', text: "x^2 + x - 1", isCorrect: false },
      { id: '3', text: "x^2 - 1", isCorrect: false }
  ]}
/>


<PythonDiagram
  client:load
  title="Exploration sandbox"
  description="Adjust sample data and parameters to connect the theory in this module to numeric behavior."
  code={`import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 200)
y = np.sin(x)

plt.plot(x, y)
plt.title("Reusable analysis sandbox")
plt.xlabel("x")
plt.ylabel("f(x)")
plt.grid(True, alpha=0.3)
plt.tight_layout()`}
/>


## References & Further Reading
- [OpenStax Mathematics resources](https://openstax.org/subjects/math) (open license)
- [MIT OpenCourseWare — Mathematics](https://ocw.mit.edu/courses/mathematics/)
- [Paul's Online Math Notes](https://tutorial.math.lamar.edu/)
