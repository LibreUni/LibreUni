---
title: Basis and Dimension
course: math-linear-algebra
order: 4
module: "Vector Spaces"
description: Spanning sets, linear independence, and the dimension of vector spaces.
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';

{/* Source tracking: Lesson narrative synthesized from authoritative documentation and educational references; see References & Further Reading. */}

# Basis and Dimension

The concepts of basis and dimension provide a way to "measure" the size and complexity of a vector space.

## Linear Independence and Spanning

A set of vectors $\{\mathbf{v}_1, \dots, \mathbf{v}_n\}$ is **linearly independent** if the only solution to $c_1\mathbf{v}_1 + \dots + c_n\mathbf{v}_n = \mathbf{0}$ is $c_i = 0$ for all $i$.
The **span** of a set is the set of all possible linear combinations.

<CodeRunner client:load 
  code={`import numpy as np

def check_linear_independence(vectors):
    matrix = np.array(vectors).T
    rank = np.linalg.matrix_rank(matrix)
    is_independent = rank == len(vectors)
    return is_independent, rank

# Test linearly independent vectors
v1 = [1, 0, 0]
v2 = [0, 1, 0]
print(f"Independent: {check_linear_independence([v1, v2])}")

# Test linearly dependent vectors
v3 = [1, 1, 0]
print(f"Independent with v3: {check_linear_independence([v1, v2, v3])}")`}
/>

## Definition of a Basis

A **basis** $B$ for a vector space $V$ is a set of vectors that:
1. Is linearly independent.
2. Spans $V$.

<Quiz client:load 
  question="How many vectors are in any basis of R^3?"
  options={[
      { id: 'a', text: "Exactly 3.", isCorrect: true },
      { id: 'b', text: "At least 3.", isCorrect: false },
      { id: 'c', text: "Any number.", isCorrect: false }
  ]}
/>

## Dimension

The **dimension** of a vector space $V$, denoted $\dim(V)$, is the number of vectors in any basis for $V$.

<CodeRunner client:load 
  code={`import numpy as np

def get_dimension(matrix):
    # The dimension of the column space is the rank
    return np.linalg.matrix_rank(matrix)

A = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
print(f"Rank (Dimension of Col Space) of A: {get_dimension(A)}")
print("A rank of 2 means the vectors are coplanar in R^3.")`}
/>

<Quiz client:load 
  question="If a subspace of R^5 has dimension 0, what is in the subspace?"
  options={[
      { id: 'a', text: "The empty set.", isCorrect: false },
      { id: 'b', text: "Only the zero vector.", isCorrect: true },
      { id: 'c', text: "A single line through the origin.", isCorrect: false }
  ]}
/>

## References & Further Reading
* [MDX Docs: What is MDX?](https://mdxjs.com/docs/what-is-mdx/) (MIT-licensed docs/site)
* [Astro Docs: Content Collections](https://docs.astro.build/en/guides/content-collections/)
* [OpenStax](https://openstax.org/)
* [MIT OpenCourseWare](https://ocw.mit.edu/)

