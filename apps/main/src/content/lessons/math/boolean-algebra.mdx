---
title: Boolean Algebra
course: math-discrete
order: 10
module: "Discrete Structures"
description: Logic gates, algebraic laws of binary variables, and simplification.
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';
import TikZ from '../../../components/TikZ.astro';
import PythonDiagram from '../../../components/PythonDiagram.astro';

# Boolean Algebra

<TikZ
  cdn={true}
  code={String.raw`\begin{tikzpicture}[scale=1]
  \draw[thick, rounded corners] (0,0) rectangle (6,3);
  \node at (3,2.5) {This lesson's core structure};
  \node[draw, fill=blue!10] at (1.6,1.4) {Definitions};
  \node[draw, fill=green!10] at (3.0,1.4) {Methods};
  \node[draw, fill=orange!10] at (4.4,1.4) {Applications};
\end{tikzpicture}`}
/>

{/* Source tracking: Core definitions and theorem statements in this lesson were synthesized from standard open educational references and should be refined with topic-specific primary sources during future maintenance. */}


Boolean algebra is the algebraic structure that handles binary variables and logic operations. It is the mathematical foundation of digital circuit design.

## Basic Operations

The primary operations are:
- **AND** ($\cdot$ or $\land$): $1 \cdot 1 = 1$, else $0$.
- **OR** ($+$ or $\lor$): $0 + 0 = 0$, else $1$.
- **NOT** ($\bar{x}$ or $\neg x$): $\bar{1} = 0, \bar{0} = 1$.

<CodeRunner client:load 
  code={`def boolean_table(func):
    print("A | B | Result")
    print("---------")
    for a in [0, 1]:
        for b in [0, 1]:
            print(f"{a} | {b} | {func(a, b)}")

# XOR Implementation
xor_func = lambda a, b: (a or b) and not (a and b)
boolean_table(xor_func)`}
/>

## Algebraic Laws

1. **Idempotence**: $x + x = x, x \cdot x = x$.
2. **De Morgan's Laws**: $\overline{x + y} = \bar{x} \cdot \bar{y}$ and $\overline{x \cdot y} = \bar{x} + \bar{y}$.
3. **Absorption**: $x + (x \cdot y) = x$.

<CodeRunner client:load 
  code={`def de_morgan_test(x, y):
    lhs = not (x or y)
    rhs = (not x) and (not y)
    return lhs == rhs

print(f"De Morgan (0, 0): {de_morgan_test(0, 0)}")
print(f"De Morgan (1, 0): {de_morgan_test(1, 0)}")
print(f"De Morgan (1, 1): {de_morgan_test(1, 1)}")`}
/>

## Sum of Products (SOP) and Simplification

Every boolean function can be represented as a **Sum of Products** (canonical form). Simplification is typically done using **Karnaugh Maps** or the **Quine-McCluskey algorithm**.

<Quiz client:load 
  question="What is the result of (A + B) * (A + C) according to distribution laws?"
  options={[
      { id: '1', text: "A + (B * C)", isCorrect: true },
      { id: '2', text: "A + B + C", isCorrect: false },
      { id: '3', text: "A * B * C", isCorrect: false }
  ]}
/>

<Quiz client:load 
  question="Which gate is considered 'Universal' because it can implement any other gate?"
  options={[
      { id: '1', text: "NAND", isCorrect: true },
      { id: '2', text: "AND", isCorrect: false },
      { id: '3', text: "OR", isCorrect: false }
  ]}
/>


<PythonDiagram
  client:load
  title="Exploration sandbox"
  description="Adjust sample data and parameters to connect the theory in this module to numeric behavior."
  code={`import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 200)
y = np.sin(x)

plt.plot(x, y)
plt.title("Reusable analysis sandbox")
plt.xlabel("x")
plt.ylabel("f(x)")
plt.grid(True, alpha=0.3)
plt.tight_layout()`}
/>


## References & Further Reading
- [OpenStax Mathematics resources](https://openstax.org/subjects/math) (open license)
- [MIT OpenCourseWare — Mathematics](https://ocw.mit.edu/courses/mathematics/)
- [Paul's Online Math Notes](https://tutorial.math.lamar.edu/)
