---
title: Category Theory
course: math-foundations
order: 20
module: "Structures"
description: The abstract study of mathematical structures and their relationships, focusing on objects, morphisms, and functors.
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';
import TikZ from '../../../components/TikZ.astro';
import PythonDiagram from '../../../components/PythonDiagram.astro';

# Category Theory

<TikZ
  cdn={true}
  code={String.raw`\begin{tikzpicture}[scale=1]
  \draw[thick, rounded corners] (0,0) rectangle (6,3);
  \node at (3,2.5) {This lesson's core structure};
  \node[draw, fill=blue!10] at (1.6,1.4) {Definitions};
  \node[draw, fill=green!10] at (3.0,1.4) {Methods};
  \node[draw, fill=orange!10] at (4.4,1.4) {Applications};
\end{tikzpicture}`}
/>

{/* Source tracking: Core definitions and theorem statements in this lesson were synthesized from standard open educational references and should be refined with topic-specific primary sources during future maintenance. */}


Category theory formalizes mathematical structure and its concepts in terms of a collection of objects and arrows (morphisms).

## Categories and Morphisms

A category $\mathcal{C}$ consists of:
- A class $ob(\mathcal{C})$ of **objects**.
- A class $hom(\mathcal{C})$ of **morphisms** (arrows) between objects.
- A composition law $\circ$ that is associative and has identity morphisms.

Examples include **Set** (sets and functions), **Grp** (groups and homomorphisms), and **Top** (topological spaces and continuous maps).

<CodeRunner client:load 
  code={`class Category:
    def __init__(self, name):
        self.name = name
        self.objects = set()
        self.morphisms = {} # (A, B) -> list of f

    def add_object(self, obj):
        self.objects.add(obj)

    def add_morphism(self, source, target, name):
        if (source, target) not in self.morphisms:
            self.morphisms[(source, target)] = []
        self.morphisms[(source, target)].append(name)

# Modeling a simple Finite Category
C = Category("SimpleFinite")
C.add_object("A")
C.add_object("B")
C.add_morphism("A", "B", "f")
C.add_morphism("B", "B", "id_B")

print(f"Objects in {C.name}: {C.objects}")
print(f"Morphisms: {C.morphisms}")`}
/>

## Functors

A **Functor** $F: \mathcal{C} \to \mathcal{D}$ is a mapping that preserves the category structure:
1. It maps objects in $\mathcal{C}$ to objects in $\mathcal{D}$.
2. It maps morphisms $f: A \to B$ in $\mathcal{C}$ to morphisms $F(f): F(A) \to F(B)$ in $\mathcal{D}$.
3. It preserves identity and composition.

<Quiz client:load 
  question="What is a 'covariant' functor?"
  options={[
      { id: '1', text: "A functor that reverses the direction of arrows.", isCorrect: false },
      { id: '2', text: "A functor that preserves the direction of arrows.", isCorrect: true },
      { id: '3', text: "A functor that only works on Abelian groups.", isCorrect: false }
  ]}
/>

## Natural Transformations

A natural transformation provides a way of transforming one functor into another while respecting the internal structure of the categories involved. It is often described as a "morphism between functors."

<Quiz client:load 
  question="In the category Set, what is the 'Initial Object'?"
  options={[
      { id: '1', text: "The set with one element {*}.", isCorrect: false },
      { id: '2', text: "The empty set Ø.", isCorrect: true },
      { id: '3', text: "the set of all real numbers.", isCorrect: false }
  ]}
/>

## Yoneda Lemma

The Yoneda Lemma suggests that an object is entirely determined by its relationships (morphisms) with other objects in the category. This is a foundational result that allows one to embed any category into a category of functors.


<PythonDiagram
  client:load
  title="Exploration sandbox"
  description="Adjust sample data and parameters to connect the theory in this module to numeric behavior."
  code={`import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 200)
y = np.sin(x)

plt.plot(x, y)
plt.title("Reusable analysis sandbox")
plt.xlabel("x")
plt.ylabel("f(x)")
plt.grid(True, alpha=0.3)
plt.tight_layout()`}
/>


## References & Further Reading
- [OpenStax Mathematics resources](https://openstax.org/subjects/math) (open license)
- [MIT OpenCourseWare — Mathematics](https://ocw.mit.edu/courses/mathematics/)
- [Paul's Online Math Notes](https://tutorial.math.lamar.edu/)
