---
title: Combinatorics and Enumerative Analysis
course: math-discrete
order: 13
module: "Set Theory & Structures"
description: A rigorous study of counting, finite structures, and the generating functions that describe them.
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';
import TikZ from '../../../components/TikZ.astro';
import PythonDiagram from '../../../components/PythonDiagram.astro';

# Combinatorics and Enumerative Analysis

<TikZ
  cdn={true}
  code={String.raw`\begin{tikzpicture}[scale=1]
  \draw[thick, rounded corners] (0,0) rectangle (6,3);
  \node at (3,2.5) {This lesson's core structure};
  \node[draw, fill=blue!10] at (1.6,1.4) {Definitions};
  \node[draw, fill=green!10] at (3.0,1.4) {Methods};
  \node[draw, fill=orange!10] at (4.4,1.4) {Applications};
\end{tikzpicture}`}
/>

{/* Source tracking: Core definitions and theorem statements in this lesson were synthesized from standard open educational references and should be refined with topic-specific primary sources during future maintenance. */}


Combinatorics is the study of discrete structures. It determines the existence, count, and optimization of arrangements according to specific rules.

### Fundamental Principles

- **Addition Principle**: If events are mutually exclusive, sum the ways.
- **Multiplication Principle**: If events occur in sequence, multiply the ways.

### Permutations and Combinations

**Permutations ($P(n, k)$)**: Choosing and *ordering* $k$ elements from $n$.
$$P(n, k) = \frac{n!}{(n-k)!}$$

**Combinations ($\binom{n}{k}$)**: Choosing $k$ elements *without* regard to order.
$$\binom{n}{k} = \frac{n!}{k!(n-k)!}$$

<CodeRunner 
  client:load 
  code={`import math

def nCr(n, r):
    return math.comb(n, r)

def nPr(n, r):
    return math.perm(n, r)

print(f"Combinations (10 choose 3): {nCr(10, 3)}")
print(f"Permutations (10 permute 3): {nPr(10, 3)}")`} 
/>

### Pascal's Triangle and Binomial Expansion

The identity $\binom{n}{k} = \binom{n-1}{k-1} + \binom{n-1}{k}$ allows for recursive construction.

<CodeRunner 
  client:load 
  code={`def pascal_triangle(rows):
    triangle = [[1]]
    for i in range(1, rows):
        row = [1]
        for j in range(1, i):
            row.append(triangle[i-1][j-1] + triangle[i-1][j])
        row.append(1)
        triangle.append(row)
    return triangle

for row in pascal_triangle(6):
    print(str(row).center(30))`} 
/>

### Advanced Principles

- **Pigeonhole Principle**: If $n > m$ items are put in $m$ boxes, one box has $>1$ item.
- **Inclusion-Exclusion**: Corrects for overcounting in unions ($|A \cup B| = |A| + |B| - |A \cap B|$).

<Quiz client:load 
  question="Which principle is best suited for counting the number of permutations where no element stays in its original position?"
  options={[
    { id: '1', text: 'Pigeonhole Principle', isCorrect: false },
    { id: '2', text: 'Binomial Theorem', isCorrect: false },
    { id: '3', text: 'Inclusion-Exclusion Principle (Derangements)', isCorrect: true },
    { id: '4', text: 'Handshaking Lemma', isCorrect: false }
  ]}
/>

### Stars and Bars: Multisets

Choosing $k$ items from $n$ types with repetition: $\binom{n+k-1}{k}$.

<CodeRunner 
  client:load 
  code={`from itertools import combinations_with_replacement

# Choosing 2 items from {A, B, C} with repetition
types = ['A', 'B', 'C']
k = 2
combos = list(combinations_with_replacement(types, k))
print(f"Multisets of size {k} from {types}:")
print(combos)
print(f"Formula count: {nCr(len(types) + k - 1, k)}")`} 
/>

<Quiz 
  client:load
  question="How many ways can you arrange 5 identical objects into 3 distinct bins (including empty bins)?"
  options={[
    { id: '1', text: 'C(5, 3)', isCorrect: false },
    { id: '2', text: 'C(5+3-1, 5)', isCorrect: true },
    { id: '3', text: '5^3', isCorrect: false }
  ]}
  explanation="Using Stars and Bars: n=3, k=5. C(3+5-1, 5) = C(7, 5)."
/>


<PythonDiagram
  client:load
  title="Exploration sandbox"
  description="Adjust sample data and parameters to connect the theory in this module to numeric behavior."
  code={`import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 200)
y = np.sin(x)

plt.plot(x, y)
plt.title("Reusable analysis sandbox")
plt.xlabel("x")
plt.ylabel("f(x)")
plt.grid(True, alpha=0.3)
plt.tight_layout()`}
/>


## References & Further Reading
- [OpenStax Mathematics resources](https://openstax.org/subjects/math) (open license)
- [MIT OpenCourseWare — Mathematics](https://ocw.mit.edu/courses/mathematics/)
- [Paul's Online Math Notes](https://tutorial.math.lamar.edu/)
