---
title: "The Fundamental Theorem of Calculus"
course: math-calculus
order: 5
module: "Calculus"
description: "The bridge between rates of change and accumulation."
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';

{/* Source tracking: Lesson narrative synthesized from authoritative documentation and educational references; see References & Further Reading. */}

# The Fundamental Theorem of Calculus

The Fundamental Theorem of Calculus (FTC) is the crown jewel of mathematics. It connects two seemingly unrelated concepts: the derivative (slopes) and the integral (areas). Before this discovery, calculating areas was a tedious process of infinite sums. After the FTC, it became a simple matter of finding an anti-derivative.

## Part 1: The Area Function

The first part of the theorem states that if we define an "area function" $F(x)$ that accumulates the area under a function $f(t)$ from a fixed point $a$ to $x$:
$$ F(x) = \int_a^x f(t) \, dt $$
Then the derivative of this area function is simply the original function:
$$ F'(x) = f(x) $$

**The Intuition:** The rate at which area is being added at point $x$ is exactly equal to the "height" of the function at that point. If the function is tall, the area grows quickly. If it is zero, the area stops growing.

## Part 2: The Shortcut to Integration

The second part provides the formula we use in practice to evaluate definite integrals. If $F$ is any anti-derivative of $f$ (meaning $F' = f$), then:
$$ \int_a^b f(x) \, dx = F(b) - F(a) $$

This is revolutionary. To find the area under a curve, you don't need to draw rectangles. You just need to find the "inverse" of the derivative and plug in the endpoints.

<CodeRunner client:load 
  code={`import sympy as sp

# Define the variable and the function
x = sp.Symbol('x')
f = x**2

# Find the anti-derivative (Indefinite Integral)
F = sp.integrate(f, x)
print(f"Function: f(x) = {f}")
print(f"Anti-derivative: F(x) = {F}")

# Evaluate the definite integral from 0 to 3 using FTC Part 2
# Integral = F(3) - F(0)
a, b = 0, 3
result = F.subs(x, b) - F.subs(x, a)

print(f"\\nIntegral from {a} to {b} of x^2 dx:")
print(f"F({b}) - F({a}) = {F.subs(x, b)} - {F.subs(x, a)} = {result}")

# Verify with sympy's definite integral tool
assert result == sp.integrate(f, (x, a, b))
`}
/>

## Why "Fundamental"?

The FTC is fundamental because it shows that differentiation and integration are inverse operations. It turns a geometry problem (area) into an algebra problem (anti-derivatives). 
Every time a physicist calculates the energy lost by a falling object or an engineer calculates the total stress on a bridge, they are relying on this 300-year-old bridge between two worlds.

## Exercises

<Quiz client:load
  question="If F(x) is the integral of f(t) from 0 to x, and f(x) = cos(x), what is F'(x)?"
  options={[
    { id: 'a', text: "sin(x)", isCorrect: false },
    { id: 'b', text: "cos(x)", isCorrect: true },
    { id: 'c', text: "-sin(x)", isCorrect: false },
    { id: 'd', text: "0", isCorrect: false }
  ]}
  explanation="By FTC Part 1, the derivative of the accumulation function is the function inside the integral."
/>

<Quiz client:load
  question="What is the result of applying the FTC to calculate ∫(2x) dx from 1 to 4?"
  options={[
    { id: 'a', text: "16 - 1 = 15", isCorrect: true },
    { id: 'b', text: "8 - 2 = 6", isCorrect: false },
    { id: 'c', text: "4 - 1 = 3", isCorrect: false },
    { id: 'd', text: "16 - 4 = 12", isCorrect: false }
  ]}
  explanation="The anti-derivative of 2x is x². So the integral is (4)² - (1)² = 16 - 1 = 15."
/>

<Quiz client:load
  question="What must be true about the function f(x) for the FTC to apply on the interval [a, b]?"
  options={[
    { id: 'a', text: "It must be differentiable.", isCorrect: false },
    { id: 'b', text: "It must be continuous.", isCorrect: true },
    { id: 'c', text: "It must be a constant.", isCorrect: false },
    { id: 'd', text: "It must be increasing.", isCorrect: false }
  ]}
  explanation="Continuity is the minimal requirement to ensure the area function is well-defined and differentiable."
/>

## References & Further Reading
* [MDX Docs: What is MDX?](https://mdxjs.com/docs/what-is-mdx/) (MIT-licensed docs/site)
* [Astro Docs: Content Collections](https://docs.astro.build/en/guides/content-collections/)
* [OpenStax](https://openstax.org/)
* [MIT OpenCourseWare](https://ocw.mit.edu/)

