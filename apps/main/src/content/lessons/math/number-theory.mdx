---
title: Number Theory
course: math-foundations
order: 8
module: "Foundations"
description: "The study of integers, primality, and the fundamental properties of numbers."
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';
import TikZ from '../../../components/TikZ.astro';
import PythonDiagram from '../../../components/PythonDiagram.astro';

# Number Theory: The Queen of Mathematics

<TikZ
  cdn={true}
  code={String.raw`\begin{tikzpicture}[scale=1]
  \draw[thick, rounded corners] (0,0) rectangle (6,3);
  \node at (3,2.5) {This lesson's core structure};
  \node[draw, fill=blue!10] at (1.6,1.4) {Definitions};
  \node[draw, fill=green!10] at (3.0,1.4) {Methods};
  \node[draw, fill=orange!10] at (4.4,1.4) {Applications};
\end{tikzpicture}`}
/>

{/* Source tracking: Core definitions and theorem statements in this lesson were synthesized from standard open educational references and should be refined with topic-specific primary sources during future maintenance. */}


Number theory is the study of the set of integers $\mathbb{Z}$. While the objects of study are simple, the problems are among the most difficult and beautiful in all of mathematics.

## 1. Prime Numbers and Factorization

A prime number is an integer $p > 1$ whose only divisors are $1$ and itself. The **Fundamental Theorem of Arithmetic** states that every integer $n > 1$ has a unique prime factorization.

Primes are the "atoms" of the number system. We can find them efficiently using the **Sieve of Eratosthenes**.

<CodeRunner 
  client:load 
  code={`def sieve(n):
    primes = [True] * (n + 1)
    p = 2
    while (p * p <= n):
        if (primes[p]):
            for i in range(p * p, n + 1, p):
                primes[i] = False
        p += 1
    return [p for p in range(2, n + 1) if primes[p]]

# Find all primes up to 100
print(f"Primes up to 100: {sieve(100)}")`} 
/>

## 2. Greatest Common Divisor (GCD)

The GCD of two integers $a$ and $b$ is the largest positive integer that divides both. The **Euclidean Algorithm** calculates this by repeatedly applying the remainder operation: $\text{gcd}(a, b) = \text{gcd}(b, a \bmod b)$.

<CodeRunner 
  client:load 
  code={`def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

print(f"GCD of 48 and 18: {gcd(48, 18)}")
print(f"GCD of 101 and 103: {gcd(101, 103)}")`} 
/>

## 3. Modular Arithmetic: "Clock Math"

In modular arithmetic, we work with remainders. We say $a \equiv b \pmod{n}$ if $n$ divides $(a-b)$. This system forms a **Ring**, which is the basis for modern cryptography (like RSA).

<Quiz 
  client:load
  question="What is 17 mod 5?"
  options={[
    { id: '1', text: '1', isCorrect: false },
    { id: '2', text: '2', isCorrect: true },
    { id: '3', text: '3', isCorrect: false }
  ]}
  explanation="17 divided by 5 is 3 with a remainder of 2."
/>

## 4. Bezout's Identity and Modular Inverses

Bezout's Identity states that for any non-zero $a, b$, there exist integers $x, y$ such that $ax + by = \text{gcd}(a, b)$. We can find these coefficients using the **Extended Euclidean Algorithm**. This is how we find **Modular Inverses**, which allow us to "divide" in modular arithmetic.

<CodeRunner 
  client:load 
  code={`def extended_gcd(a, b):
    if a == 0:
        return b, 0, 1
    gcd, x1, y1 = extended_gcd(b % a, a)
    x = y1 - (b // a) * x1
    y = x1
    return gcd, x, y

# Find modular inverse of 3 mod 11
# We need x such that 3x = 1 (mod 11)
g, x, y = extended_gcd(3, 11)
if g == 1:
    print(f"Modular inverse of 3 mod 11 is: {x % 11}")`} 
/>

## 5. Summary Check

<Quiz 
  client:load
  question="If gcd(a, n) = 1, then a is said to be ___ to n."
  options={[
    { id: '1', text: 'Prime', isCorrect: false },
    { id: '2', text: 'Relatively Prime (Coprime)', isCorrect: true },
    { id: '3', text: 'Inverse', isCorrect: false }
  ]}
/>


<PythonDiagram
  client:load
  title="Exploration sandbox"
  description="Adjust sample data and parameters to connect the theory in this module to numeric behavior."
  code={`import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 200)
y = np.sin(x)

plt.plot(x, y)
plt.title("Reusable analysis sandbox")
plt.xlabel("x")
plt.ylabel("f(x)")
plt.grid(True, alpha=0.3)
plt.tight_layout()`}
/>


## References & Further Reading
- [OpenStax Mathematics resources](https://openstax.org/subjects/math) (open license)
- [MIT OpenCourseWare — Mathematics](https://ocw.mit.edu/courses/mathematics/)
- [Paul's Online Math Notes](https://tutorial.math.lamar.edu/)
