---
title: Real Numbers and Continuity
course: math-foundations
order: 6
module: "Analysis Foundations"
description: Construction of real numbers, Dedekind cuts, and the completeness axiom.
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';
import TikZ from '../../../components/TikZ.astro';
import PythonDiagram from '../../../components/PythonDiagram.astro';

# Real Numbers and Continuity

<TikZ
  cdn={true}
  code={String.raw`\begin{tikzpicture}[scale=1]
  \draw[thick, rounded corners] (0,0) rectangle (6,3);
  \node at (3,2.5) {This lesson's core structure};
  \node[draw, fill=blue!10] at (1.6,1.4) {Definitions};
  \node[draw, fill=green!10] at (3.0,1.4) {Methods};
  \node[draw, fill=orange!10] at (4.4,1.4) {Applications};
\end{tikzpicture}`}
/>

{/* Source tracking: Core definitions and theorem statements in this lesson were synthesized from standard open educational references and should be refined with topic-specific primary sources during future maintenance. */}


The real numbers $\mathbb{R}$ extend the rationals $\mathbb{Q}$ by filling in the "gaps," allowing for a continuous number line.

## Dedekind Cuts

A **Dedekind cut** $(A, B)$ is a partition of $\mathbb{Q}$ such that $A$ has no largest element and everything in $A$ is less than everything in $B$. This construction formally defines irrational numbers like $\sqrt{2}$.

<CodeRunner client:load 
  code={`def is_in_sqrt2_cut(q):
    # A = {q in Q | q < 0 or q^2 < 2}
    if q < 0: return True
    return q**2 < 2

test_rationals = [1, 1.4, 1.41, 1.414, 1.42, 1.5, 2]
for q in test_rationals:
    print(f"{q} in A: {is_in_sqrt2_cut(q)}")`}
/>

## Completeness Axiom

The **Completeness Axiom** states that every non-empty set of real numbers that is bounded above has a **Least Upper Bound** (Supremum) in $\mathbb{R}$. This is what distinguishes $\mathbb{R}$ from $\mathbb{Q}$.

<CodeRunner client:load 
  code={`import numpy as np

def find_supremum_approximation(func, bounds, steps=1000):
    x = np.linspace(bounds[0], bounds[1], steps)
    y = func(x)
    return np.max(y)

# Find supremum of -x^2 + 4 on [0, 5]
# (Should be 4 at x=0)
f = lambda x: -x**2 + 4
print(f"Approximated Supremum: {find_supremum_approximation(f, [0, 5])}")`}
/>

## Archimedean Property

The **Archimedean Property** states that for any $x \in \mathbb{R}$, there exists an $n \in \mathbb{N}$ such that $n > x$. This implies that there are no "infinitely large" real numbers.

<Quiz client:load 
  question="Does the set of rational numbers Q satisfy the completeness axiom?"
  options={[
      { id: '1', text: "No, because the supremum of {q in Q | q^2 < 2} is sqrt(2), which is not in Q.", isCorrect: true },
      { id: '2', text: "Yes, Q is a field.", isCorrect: false },
      { id: '3', text: "Only for finite sets.", isCorrect: false }
  ]}
/>

## The Cantor Set

The **Cantor Set** is constructed by repeatedly removing the middle third of line segments. It is a classic example of a set that is uncountable but has "measure zero."

<Quiz client:load 
  question="What is the cardinality of the set of real numbers R?"
  options={[
      { id: '1', text: "Aleph-zero (Countable)", isCorrect: false },
      { id: '2', text: "C (Continuum)", isCorrect: true },
      { id: '3', text: "Finite", isCorrect: false }
  ]}
/>


<PythonDiagram
  client:load
  title="Exploration sandbox"
  description="Adjust sample data and parameters to connect the theory in this module to numeric behavior."
  code={`import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 200)
y = np.sin(x)

plt.plot(x, y)
plt.title("Reusable analysis sandbox")
plt.xlabel("x")
plt.ylabel("f(x)")
plt.grid(True, alpha=0.3)
plt.tight_layout()`}
/>


## References & Further Reading
- [OpenStax Mathematics resources](https://openstax.org/subjects/math) (open license)
- [MIT OpenCourseWare — Mathematics](https://ocw.mit.edu/courses/mathematics/)
- [Paul's Online Math Notes](https://tutorial.math.lamar.edu/)
