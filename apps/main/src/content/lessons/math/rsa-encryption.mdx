---
title: RSA Encryption
course: math-foundations
order: 11
module: "Foundations"
description: "Understanding the mathematical foundations of RSA public-key cryptography using modular arithmetic."
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';
import TikZ from '../../../components/TikZ.astro';
import PythonDiagram from '../../../components/PythonDiagram.astro';

# RSA Encryption

<TikZ
  cdn={true}
  code={String.raw`\begin{tikzpicture}[scale=1]
  \draw[thick, rounded corners] (0,0) rectangle (6,3);
  \node at (3,2.5) {This lesson's core structure};
  \node[draw, fill=blue!10] at (1.6,1.4) {Definitions};
  \node[draw, fill=green!10] at (3.0,1.4) {Methods};
  \node[draw, fill=orange!10] at (4.4,1.4) {Applications};
\end{tikzpicture}`}
/>

{/* Source tracking: Core definitions and theorem statements in this lesson were synthesized from standard open educational references and should be refined with topic-specific primary sources during future maintenance. */}


RSA (Rivest–Shamir–Adleman) is the algorithmic backbone of modern digital security. It relies on the practical difficulty of factoring the product of two large prime numbers.

## 1. The Key Generation Process

The security of RSA rests on three main steps:
1. Choose two large primes $p$ and $q$.
2. Compute $n = pq$. This $n$ is used as the modulus for both public and private keys.
3. Compute Euler's totient function $\phi(n) = (p-1)(q-1)$.
4. Choose an integer $e$ such that $1 < e < \phi(n)$ and $\gcd(e, \phi(n)) = 1$.
5. Determine $d$ as $d \equiv e^{-1} \pmod{\phi(n)}$.

<CodeRunner 
  client:load 
  code={`def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

def extended_gcd(a, b):
    if a == 0: return b, 0, 1
    d, x1, y1 = extended_gcd(b % a, a)
    x = y1 - (b // a) * x1
    y = x1
    return d, x, y

def mod_inverse(e, phi):
    d, x, y = extended_gcd(e, phi)
    return x % phi

p, q = 61, 53
n = p * q
phi = (p-1) * (q-1)
e = 65537 # Common choice for e
d = mod_inverse(e, phi)

print(f"Public Key (e, n): ({e}, {n})")
print(f"Private Key (d, n): ({d}, {n})")`} 
/>

## 2. Encryption and Decryption

A message $M$ is encrypted into ciphertext $C$ using the public key $(e, n)$:
$$C \equiv M^e \pmod n$$

The original message is recovered using the private key $(d, n)$:
$$M \equiv C^d \pmod n$$

<CodeRunner 
  client:load 
  code={`# Use the keys from the previous step
p, q = 61, 53
n, e = p*q, 65537
d = 2753 # Computed in step 1

def encrypt(m, e, n):
    return pow(m, e, n)

def decrypt(c, d, n):
    return pow(c, d, n)

message = 42
ciphertext = encrypt(message, e, n)
decrypted = decrypt(ciphertext, d, n)

print(f"Original: {message}")
print(f"Encrypted: {ciphertext}")
print(f"Decrypted: {decrypted}")`} 
/>

## 3. Why it Works (Euler’s Theorem)

RSA works because $M^{ed} \equiv M^{k\phi(n) + 1} \equiv M \pmod n$. According to **Euler’s Totient Theorem**, if $\gcd(M, n) = 1$, then $M^{\phi(n)} \equiv 1 \pmod n$. 

<Quiz 
  client:load
  question="What happens if an attacker finds out p and q?"
  options={[
    { id: '1', text: 'They can compute phi(n) and then find the private key d.', isCorrect: true },
    { id: '2', text: 'Nothing, the public key is still safe.', isCorrect: false },
    { id: '3', text: 'They only get half of the code.', isCorrect: false }
  ]}
  explanation="Knowing p and q allows anyone to compute phi(n) = (p-1)(q-1), and then compute the modular inverse of e to find d."
/>

## 4. Key Takeaways

- **One-way function**: Multiplication of primes is easy, but factoring their product is hard.
- **Asymmetric**: You share the public key with everyone but keep the private key secret.

<Quiz 
  client:load
  question="Which value is part of the public key?"
  options={[
    { id: '1', text: 'p and q', isCorrect: false },
    { id: '2', text: 'd', isCorrect: false },
    { id: '3', text: 'e and n', isCorrect: true }
  ]}
/>


<PythonDiagram
  client:load
  title="Exploration sandbox"
  description="Adjust sample data and parameters to connect the theory in this module to numeric behavior."
  code={`import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 200)
y = np.sin(x)

plt.plot(x, y)
plt.title("Reusable analysis sandbox")
plt.xlabel("x")
plt.ylabel("f(x)")
plt.grid(True, alpha=0.3)
plt.tight_layout()`}
/>


## References & Further Reading
- [OpenStax Mathematics resources](https://openstax.org/subjects/math) (open license)
- [MIT OpenCourseWare — Mathematics](https://ocw.mit.edu/courses/mathematics/)
- [Paul's Online Math Notes](https://tutorial.math.lamar.edu/)
