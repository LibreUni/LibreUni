---
title: Linear Transformations and Matrices
course: math-linear-algebra
order: 7
module: "Linear Maps"
description: Mapping between vector spaces, matrix representation, and coordinate changes.
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';

{/* Source tracking: Lesson narrative synthesized from authoritative documentation and educational references; see References & Further Reading. */}

# Linear Transformations and Matrices

A linear transformation $T: V \to W$ is a mapping between vector spaces that preserves the operations of addition and scalar multiplication.

## Properties of Linear Maps

$T$ is linear if for all $\mathbf{u}, \mathbf{v} \in V$ and $c \in F$:
1. $T(\mathbf{u} + \mathbf{v}) = T(\mathbf{u}) + T(\mathbf{v})$
2. $T(c\mathbf{v}) = cT(\mathbf{v})$

Every linear transformation between finite-dimensional vector spaces can be represented as a **matrix**.

<CodeRunner client:load 
  code={`import numpy as np

def apply_transformation(A, v):
    return np.dot(A, v)

# Rotation Matrix by 90 degrees (pi/2)
theta = np.pi / 2
R = np.array([[np.cos(theta), -np.sin(theta)],
              [np.sin(theta),  np.cos(theta)]])

v = np.array([1, 0])
v_rot = apply_transformation(R, v)

print(f"Original: {v}")
print(f"Rotated 90 deg: {v_rot.round(2)}")`}
/>

## Composition and Matrix Multiplication

If $T: U \to V$ and $S: V \to W$ are linear maps, their composition $S \circ T: U \to W$ is also linear. The matrix representing $S \circ T$ is the product of the matrices representing $S$ and $T$.

<Quiz client:load 
  question="If A is a 3x2 matrix and B is a 2x5 matrix, what is the size of AB?"
  options={[
      { id: '1', text: "2x2", isCorrect: false },
      { id: '2', text: "3x5", isCorrect: true },
      { id: '3', text: "5x3", isCorrect: false }
  ]}
/>

## Change of Basis

The matrix representation of a linear map depends on the choice of bases for $V$ and $W$. If $[T]_B$ is the matrix in basis $B$, and $P$ is the transition matrix from basis $B'$ to $B$, then:
$$[T]_{B'} = P^{-1} [T]_B P$$

<CodeRunner client:load 
  code={`# T in standard basis
T_std = np.array([[1, 2], [3, 4]])

# New basis B' = {[1,1], [1,0]}
P = np.array([[1, 1], [1, 0]])
P_inv = np.linalg.inv(P)

# T in new basis
T_new = P_inv @ T_std @ P
print(f"Matrix in new basis:\\n{T_new.round(2)}")`}
/>

<Quiz client:load 
  question="What type of matrix transforms any vector to itself?"
  options={[
      { id: '1', text: "Zero Matrix", isCorrect: false },
      { id: '2', text: "Identity Matrix", isCorrect: true },
      { id: '3', text: "Orthogonal Matrix", isCorrect: false }
  ]}
/>

## References & Further Reading
* [MDX Docs: What is MDX?](https://mdxjs.com/docs/what-is-mdx/) (MIT-licensed docs/site)
* [Astro Docs: Content Collections](https://docs.astro.build/en/guides/content-collections/)
* [OpenStax](https://openstax.org/)
* [MIT OpenCourseWare](https://ocw.mit.edu/)

