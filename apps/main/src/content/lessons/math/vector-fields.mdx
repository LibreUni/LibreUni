---
title: "Vector Fields"
course: math-calculus
order: 7
module: "Multivariable Calculus"
description: "Mapping vectors to every point in space: the language of physics."
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';

{/* Source tracking: Lesson narrative synthesized from authoritative documentation and educational references; see References & Further Reading. */}

# Vector Fields

In single-variable calculus, we map a number to a number ($x \to y$). In **Vector Calculus**, we map a position to a vector ($\mathbf{r} \to \mathbf{V}$). This represents physical phenomena like wind speed at every point in a city, or the force of gravity at every point in the solar system.

## The Gradient: The Direction of Maximum Ascent

Given a scalar field $f(x, y)$ (like the altitude of a mountain), the **Gradient** $\nabla f$ is a vector field that points in the direction of the steepest uphill slope.

$$\nabla f = \left( \frac{\partial f}{\partial x}, \frac{\partial f}{\partial y} \right)$$

The magnitude of the gradient tells you how steep the slope is.

## Divergence: Sources and Sinks

The **Divergence** ($\nabla \cdot \mathbf{V}$) of a vector field measures how much the "fluid" is expanding or compressing at a point.
- **Positive Divergence**: The point is a **Source** (fluid is created/flowing out).
- **Negative Divergence**: The point is a **Sink** (fluid is destroyed/flowing in).

## Curl: The Rotation of the Field

The **Curl** ($\nabla \times \mathbf{V}$) measures the tendency of the field to rotate around a point. If you placed a tiny paddlewheel in the field, the curl tells you how fast and in what direction it would spin.

<CodeRunner client:load 
  code={`import numpy as np
import matplotlib.pyplot as plt

# Define a grid of points
x, y = np.meshgrid(np.linspace(-2, 2, 10), np.linspace(-2, 2, 10))

# Example Vector Field: Rotational field (V = [-y, x])
u = -y
v = x

print("Visualizing a rotational field (Curl > 0):")
# In a real environment, we'd use plt.quiver(x, y, u, v)
# For now, let's look at the vectors at a few points
for i in [0, 5, 9]:
    for j in [0, 5, 9]:
        print(f"Point ({x[i,j]:.1f}, {y[i,j]:.1f}) -> Vector ({u[i,j]:.1f}, {v[i,j]:.1f})")
`}
/>

## Conservative Fields

A vector field is **Conservative** if it is the gradient of some scalar function ($V = \nabla f$). In physics, this means the work done moving between two points is independent of the path taken. Gravity and electric fields are conservative; friction is not.

## Exercises

<Quiz client:load
  question="If the Divergence of a wind field at a certain point is zero, what does it mean?"
  options={[
    { id: 'a', text: "There is no wind.", isCorrect: false },
    { id: 'b', text: "The wind is blowing in a circle.", isCorrect: false },
    { id: 'c', text: "The air is incompressible; as much air enters the point as leaves it.", isCorrect: true },
    { id: 'd', text: "The wind is getting faster.", isCorrect: false }
  ]}
  explanation="Zero divergence means the flow is 'solenoidal'—there are no net sources or sinks of fluid at that point."
/>

<Quiz client:load
  question="What does the Curl of a field represent?"
  options={[
    { id: 'a', text: "The change in density.", isCorrect: false },
    { id: 'b', text: "The local rotation of the field.", isCorrect: true },
    { id: 'c', text: "The total flux through a surface.", isCorrect: false },
    { id: 'd', text: "The distance from the origin.", isCorrect: false }
  ]}
  explanation="Curl measures the 'vorticity' or spinning motion of the field at a specific point."
/>

<Quiz client:load
  question="Why is the Gradient important in Machine Learning?"
  options={[
    { id: 'a', text: "It helps define the data types.", isCorrect: false },
    { id: 'b', text: "It is used in Graduate Descent to find the minimum of an error function.", isCorrect: true },
    { id: 'c', text: "It creates the neural network structure.", isCorrect: false },
    { id: 'd', text: "It sorts the data.", isCorrect: false }
  ]}
  explanation="In Gradient Descent, we calculate the gradient of the 'Loss' function and move in the *opposite* direction to find the lowest error possible."
/>

## References & Further Reading
* [MDX Docs: What is MDX?](https://mdxjs.com/docs/what-is-mdx/) (MIT-licensed docs/site)
* [Astro Docs: Content Collections](https://docs.astro.build/en/guides/content-collections/)
* [OpenStax](https://openstax.org/)
* [MIT OpenCourseWare](https://ocw.mit.edu/)

