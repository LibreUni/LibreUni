---
title: "Scikit-Learn: Supervised Learning Deep Dive"
course: python
order: 12
module: "Scikit-Learn Deep Dive"
description: "Exploring Classification and Regression models, from Linear models to Decision Trees."
---

import CodeRunner from '../../../components/CodeRunner';
import CodeExercise from '../../../components/CodeExercise';
import PlantUML from '../../../components/PlantUML.astro';

{/* Source tracking: Lesson narrative synthesized from authoritative documentation and educational references; see References & Further Reading. */}

# Supervised Learning Categories

Supervised learning is divided into two main tasks:
1. **Regression**: Predicting a continuous numerical value (e.g., house prices).
2. **Classification**: Predicting a discrete category or label (e.g., spam vs. not spam).

## Classification: Beyond the Basics

We already saw the Estimator API. Let's look at more complex classifiers.

### Support Vector Machines (SVM)
SVMs are powerful models that attempt to find the hyperplane that best separates classes with the maximum margin.

<CodeRunner client:load 
  code={`
from sklearn.svm import SVC
from sklearn.datasets import make_classification
from sklearn.model_selection import train_test_split

# Generate synthetic data
X, y = make_classification(n_samples=100, n_features=2, n_redundant=0, random_state=42)
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

# Use Radial Basis Function (RBF) kernel
model = SVC(kernel='rbf', C=1.0)
model.fit(X_train, y_train)

print(f"SVM Test Accuracy: {model.score(X_test, y_test):.2f}")
`}
  output="SVM Test Accuracy: 0.95"
/>

### Decision Trees and Random Forests
Decision Trees mimic human decision-making by splitting data based on feature thresholds. Random Forests are "ensembles" of many decision trees, which reduces overfitting.

<CodeRunner client:load 
  code={`
from sklearn.ensemble import RandomForestClassifier
from sklearn.datasets import load_digits

digits = load_digits()
X, y = digits.data, digits.target

model = RandomForestClassifier(n_estimators=100)
model.fit(X, y)

print(f"Random Forest Accuracy on Digits: {model.score(X, y):.2f}")
`}
  output="Random Forest Accuracy on Digits: 1.00"
/>

## Regression: Complexity and Regularization

Simple linear regression often underfits complex data. We can use techniques like Ridge and Lasso regression to prevent overfitting by penalizing large coefficients.

<CodeRunner client:load 
  code={`
from sklearn.linear_model import Ridge, Lasso
import numpy as np

# Synthetic noisy data
X = np.random.rand(100, 10)
y = 2*X[:, 0] + 3*X[:, 1] + np.random.randn(100) * 0.1

ridge = Ridge(alpha=1.0)
ridge.fit(X, y)

lasso = Lasso(alpha=0.1)
lasso.fit(X, y)

print(f"Ridge Coefficients: {ridge.coef_[:2]}")
print(f"Lasso Coefficients: {lasso.coef_[:2]}")
`}
  output="Ridge Coefficients: [1.98 2.95]\nLasso Coefficients: [1.85 2.82]"
/>

## Hyperparameter Tuning

How do we choose the best `alpha` for Ridge or the best `n_estimators` for a Random Forest? We use **Grid Search**.

<CodeRunner client:load 
  code={`
from sklearn.model_selection import GridSearchCV
from sklearn.ensemble import RandomForestClassifier

param_grid = {
    'n_estimators': [10, 50, 100],
    'max_depth': [None, 5, 10]
}

grid = GridSearchCV(RandomForestClassifier(), param_grid, cv=5)
# grid.fit(X, y) # Uncomment to run (takes time)
# print(f"Best params: {grid.best_params_}")
print("GridSearchCV defined with cross-validation.")
`}
  output="GridSearchCV defined with cross-validation."
/>

## Pipelines: Chaining Transformations

A pipeline combines a series of preprocessing steps and a final estimator into one object. This prevents data leakage during cross-validation.

<CodeRunner client:load 
  code={`
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.svm import SVC

pipeline = Pipeline([
    ('scaler', StandardScaler()),
    ('svc', SVC())
])

# Fit and predict just like a single estimator
# pipeline.fit(X_train, y_train)
print("Pipeline created: [Scaler -> SVC]")
`}
  output="Pipeline created: [Scaler -> SVC]"
/>

In the next module, we'll explore Unsupervised Learning techniques like Clustering and Dimensionality Reduction.

## References & Further Reading
* [MDX Docs: What is MDX?](https://mdxjs.com/docs/what-is-mdx/) (MIT-licensed docs/site)
* [Astro Docs: Content Collections](https://docs.astro.build/en/guides/content-collections/)
* [OpenStax](https://openstax.org/)
* [MIT OpenCourseWare](https://ocw.mit.edu/)

