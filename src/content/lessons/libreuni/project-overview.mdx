---
title: "LibreUni Project Overview"
course: libreuni
description: "What LibreUni is, how the platform is organized, and where contributors can make useful changes."
---
import PlantUML from '../../../components/PlantUML.astro';
import Quiz from '../../../components/Quiz';
import CaseStudy from '../../../components/CaseStudy';

# LibreUni Project Overview

LibreUni is a free learning platform built around plain content, static rendering, and interactive components. Lessons are written as MDX, courses are described with small JSON files, and the app turns that material into searchable course pages, lesson pages, exports, exercises, diagrams, and code labs.

The project is intentionally contributor-friendly. A useful change can be a new lesson, a better explanation, a fixed diagram, a more focused quiz, a clearer code sample, or an accessibility improvement.

## Repository Map

<PlantUML theme="none" code={`
skinparam backgroundColor transparent
skinparam componentStyle rectangle

package "LibreUni" {
  folder "src" {
    [Astro pages] as pages
    [MDX lessons] as lessons
    [Course JSON] as courses
    [Interactive components] as components
  }

  folder "tests" {
    [Smoke tests] as smoke
    [Accessibility tests] as accessibility
    [UX checks] as ux
  }

  folder "docs" {
    [Authoring rules] as rules
    [PlantUML notes] as puml
  }
}

courses --> pages : course cards
lessons --> pages : lesson routes
components --> lessons : embedded labs
tests --> pages : verify rendered app
docs --> lessons : author guidance
`} />

Click the diagram to open the overlay. Large diagrams should keep their proportions, scale down when needed, and expand cleanly on demand.

## What Contributors Usually Touch

| Area | Common files | Good contribution shape |
| :--- | :--- | :--- |
| Course metadata | `src/content/courses/*.json` | Clear title, description, icon, color, image |
| Lesson content | `src/content/lessons/**.mdx` | Focused lesson with examples, checks, and references |
| Interactive widgets | `src/components/*` | Reusable feature with stable props |
| Build and tests | `tests/**`, `tools/**` | Verifies behavior without slowing normal authorship |
| Documentation | `docs/**`, `README.md` | Helps the next contributor make fewer guesses |

## Contribution Mindset

A good LibreUni contribution is small enough to review, rich enough to teach, and boring enough to maintain. Prefer one clear idea per lesson section. Use visual and interactive elements when they make the concept easier to understand, not as decoration.

<CaseStudy client:load
  title="Choosing a First Change"
  scenario="A new contributor notices that a lesson explains state machines only in prose. They can either rewrite the entire UML course, add one focused diagram and quiz to the state-machine lesson, or start by changing the homepage layout."
  question="Which first contribution is easiest to review and most aligned with the project?"
  options={[
    "Rewrite the entire UML course in one pull request",
    "Add one focused state-machine diagram and a small quiz to the existing lesson",
    "Change the homepage first because it is more visible",
    "Avoid content changes until the whole design system is redesigned"
  ]}
  correctIndex={1}
  explanation="A focused lesson improvement has a clear intent, a small review surface, and immediate learner value. Bigger redesigns can happen later when the contributor understands more of the project."
/>

## Quick Check

<Quiz client:load
  title="Project Orientation"
  question="What is the main authoring format for LibreUni lessons?"
  options={["Plain text files with no components", "MDX files that can embed interactive components", "Only JSON documents", "Compiled PDFs"]}
  correctIndex={1}
  explanation="Lessons are MDX, which means authors can combine Markdown, math, code fences, and imported Astro or React components."
/>
