---
title: "Relations, Functions, and Morphisms"
course: math-discrete
order: 10
module: "Set Theory & Structures"
description: An analytical study of mappings, equivalence relations, and the structural properties of functions.
---

import Quiz from '../../../components/Quiz';
import CodeRunner from '../../../components/CodeRunner';

# Relations, Functions, and Morphisms

Mappings allow us to transition from static sets to dynamic interactions. They allow us to compare sets, group elements by shared properties, and transform data while preserving structure.

### Binary Relations

A **Binary Relation** $R$ between sets $A$ and $B$ is a subset of the Cartesian product $A \times B$. If $(a, b) \in R$, we write $aRb$.

A relation $R$ on set $A$ can be:
- **Reflexive**: $aRa$.
- **Symmetric**: $aRb \implies bRa$.
- **Transitive**: $aRb \land bRc \implies aRc$.

<CodeRunner 
  client:load 
  code={`def check_properties(elements, relation):
    reflexive = all((e, e) in relation for e in elements)
    symmetric = all((b, a) in relation for (a, b) in relation)
    transitive = all((a, c) in relation 
                     for (a, b1) in relation 
                     for (b2, c) in relation if b1 == b2)
    return {"Reflexive": reflexive, "Symmetric": symmetric, "Transitive": transitive}

S = {1, 2, 3}
R = {(1, 1), (2, 2), (3, 3), (1, 2), (2, 1)}
print(f"Set: {S}, Relation: {R}")
print(f"Properties: {check_properties(S, R)}")`} 
/>

### Equivalence Relations and Partitions

A relation that is reflexive, symmetric, and transitive is an **Equivalence Relation**. It partitions a set into disjoint **Equivalence Classes**.

<CodeRunner 
  client:load 
  code={`def get_equivalence_classes(elements, relation):
    classes = []
    seen = set()
    for e in elements:
        if e not in seen:
            # Class [e] = {x | xRe}
            cls = {x for x in elements if (x, e) in relation}
            classes.append(cls)
            seen.update(cls)
    return classes

print(f"Equivalence Classes: {get_equivalence_classes(S, R)}")`} 
/>

### Classification of Mappings

A function $f: A \to B$ is a relation where every input has exactly one output.

1.  **Injection (One-to-One)**: $f(x) = f(y) \implies x = y$.
2.  **Surjection (Onto)**: Range equals Codomain.
3.  **Bijection**: Both injective and surjective (invertible).

<CodeRunner 
  client:load 
  code={`def classify_function(domain, codomain, f_map):
    # f_map is a dict {x: y}
    injective = len(set(f_map.values())) == len(f_map)
    surjective = set(f_map.values()) == codomain
    
    if injective and surjective: return "Bijection"
    if injective: return "Injection"
    if surjective: return "Surjection"
    return "General Function"

A = {1, 2, 3}
B = {'a', 'b', 'c'}
f = {1: 'a', 2: 'b', 3: 'c'}
print(f"Function mapping: {f}")
print(f"Classification: {classify_function(A, B, f)}")`} 
/>

### Morphisms: Structural Preservation

In abstract algebra, we study functions that preserve operations:
- **Homomorphism**: Preserves algebraic structure $(f(a \cdot b) = f(a) \cdot f(b))$.
- **Isomorphism**: A bijective homomorphism.

<Quiz 
    client:load
    question="A relation that is reflexive, symmetric, and transitive is known as what?"
    options={[
        { id: '1', text: 'Partial Order', isCorrect: false },
        { id: '2', text: 'Total Order', isCorrect: false },
        { id: '3', text: 'Equivalence Relation', isCorrect: true },
        { id: '4', text: 'Surjection', isCorrect: false }
    ]}
/>

### Pre-images and Images

For a subset $T \subseteq B$, the **Pre-image** is $f^{-1}(T) = \{x \in A \mid f(x) \in T\}$.

<Quiz 
  client:load
  question="If f: A -> B is an injection, and |A| = |B| is finite, what must f also be?"
  options={[
    { id: '1', text: 'Constant', isCorrect: false },
    { id: '2', text: 'Surjection (and thus Bijection)', isCorrect: true },
    { id: '3', text: 'An equivalence relation', isCorrect: false }
  ]}
  explanation="According to the Pigeonhole Principle, if we map n elements to n destinations without overlap, we must hit every destination."
/>
